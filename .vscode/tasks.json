{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-alias-test",
      "type": "shell",
      "command": "npx tsc -p tsconfig.test.json && npx tsc-alias -p tsconfig.test.json",
      "options": {
        "shell": {
          "executable": "cmd.exe",
          "args": ["/d", "/c"]
        }
      },
      "problemMatcher": []
    }
  ]
}
